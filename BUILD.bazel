load("@gazelle//:def.bzl", "gazelle")

###
# Enable gazelle
#

# gazelle:prefix velox.dev
gazelle(name = "gazelle")

####
# Aliases for easier builds
#

alias(
    name = "velox_server",
    actual = "//cmd:velox_server",
)
